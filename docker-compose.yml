services:
  rabbitmq:
    image: rabbitmq:3-management
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"

  postgres-transactions:
    image: postgres:17
    environment:
      POSTGRES_DB: transactions_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5434:5432"
    volumes:
      - transactions_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d

  postgres-webhooks:
    image: postgres:17
    environment:
      POSTGRES_DB: webhook_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5435:5432"
    volumes:
      - webhooks_data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d

  mock-bank-service:
    image: ghcr.io/onyxpayments/mock-bank-service:latest
    ports:
      - "8001:8000"

  payment-orchestator-service:
    image: ghcr.io/onyxpayments/payment-orchestrator-service:latest
    ports:
      - "8002:8001"
    environment:
      MOCK_BANK_BASE_URL: http://mock-bank-service:8000
    depends_on:
      - mock-bank-service

volumes:
  transactions_data:
  webhooks_data: